# 🐍 API FastAPI com PostgreSQL + Render (Simplificada)

## 🚀 Executar Localmente
```bash
git clone https://github.com/ketsiane/Avaliacao
cd fastapi-render
python -m venv .venv
.venv\Scripts\activate  # Windows
pip install -r requirements.txt
